# 写操作一致性策略

- 云函数对每个写操作重新读取账本并判定角色，客户端参数不作为权限依据。
- 记录写入校验金额、类型、日期和标签数量；分类删除和账本删除校验目标 ID。
- 邀请领取属于少量文档修改，应使用 CloudBase 事务或条件更新，保证令牌只能绑定到一个 OpenID。
- 账本删除、分类迁移可能影响超过单事务容量的记录。此类操作必须采用“标记进行中 → 分批处理 → 完成确认”的可恢复流程，不能假装使用单个无限事务。
- 生产环境启用前，必须在测试环境演练中断、重试和重复请求；备份/导出流程按项目决定暂不纳入当前开发库。
