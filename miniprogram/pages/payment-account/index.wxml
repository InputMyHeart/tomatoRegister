<view class="page plain-page payment-account-page">
  <tl-navbar title="支付账户" showBack="{{true}}">
  </tl-navbar>
  <scroll-view class="page-scroll" scroll-y="{{true}}" enhanced="{{true}}" show-scrollbar="{{false}}">
    <view class="page-content">
      <view class="account-card card fade-up">
        <view class="account-header">
          <view>
            <view class="account-title">{{ledgerName}}</view>
            <view class="account-subtitle">记一笔时可选择以下支付账户</view>
          </view>
          <tl-icon name="bank-card-line" size="42" color="#f0442f">
          </tl-icon>
        </view>
        <view class="account-list">
          <view wx:for="{{accounts}}" wx:key="*this" class="account-row">
            <view class="account-icon">
              <tl-icon name="wallet-3-line" size="32" color="#f0442f">
              </tl-icon>
            </view>
            <text class="account-name">{{item}}</text>
            <text wx:if="{{!readonly}}" class="delete-text" data-index="{{index}}" bindtap="removeAccount">删除</text>
          </view>
        </view>
      </view>
      <view wx:if="{{readonly}}" class="readonly-tip">访客不能管理支付账户</view>
    </view>
  </scroll-view>
  <view wx:if="{{!readonly}}" class="account-add-bar">
    <button class="tomato-button" disabled="{{saving}}" bindtap="addAccount">
      <tl-icon name="add-line" size="32" color="#ffffff">
      </tl-icon>
      <text>{{saving ? '保存中...' : '新增账户'}}</text>
    </button>
  </view>
</view>
